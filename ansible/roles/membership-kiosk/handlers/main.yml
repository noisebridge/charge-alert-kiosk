---
- name: Restart kiosk
  ansible.builtin.systemd:
    name: kiosk
    state: restarted
    daemon_reload: true

- name: Restart upcoming-events
  ansible.builtin.systemd:
    name: upcoming-events
    state: restarted
    daemon_reload: true

- name: Reboot
  ansible.builtin.reboot:
